spring:
  datasource:
    url: jdbc:h2:mem:userdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  docker:
    compose:
      enabled: false

server:
  port: 8000


app:
  webauthn:
    rp-id: localhost
    rp-name: Frostbyte User Service
    allowed-origins:
      - http://localhost:8000