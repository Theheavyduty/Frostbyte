// Say to Alex if we need to call it User or Child.
export interface ChildData{
    id: string, // Same as parent ID type question.
    name: string
    // We need a right child model from backend?
}
